you can check credential result!

```tsx
const claims = {
  firstname: "John",
  lastname: "Doe",
  ssn: "123-45-6789",
  id: "1234",
};

const disclosureFrame = {
  _sd: ["firstname", "id"],
};

const credential = await sdjwt.issue(claims, disclosureFrame);
//you can get a token
```
