```tsx
const presentationFrame = ["firstname", "id"];
const presentation = await sdjwt.present(credential, presentationFrame);
//The credential in the first argument is the token you were initially issued.
```
